<%= link_to('', icon_path(resource, :edit),
            title: t('edit'), class: 'button-edit') %>

<%= link_to('', resource,
            remote: true,
            method: :delete,
            data: { confirm: t(:realy_delete) },
            title: t('destroy'), class: 'button-delete') %>

<%= link_to('', icon_path(resource, :move_up),
            remote: true,
            method: :patch,
            title: t('move.up'), class: 'button-up') %>

<%= link_to('', icon_path(resource, :move_down),
            remote: true,
            method: :patch,
            title: t('move.down'), class: 'button-down') %>
