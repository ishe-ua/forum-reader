<%= content_tag(:li, letter_item, class: 'letter-settings') do %>
  <div class="item">
    <span class="name">
      <%= link_to letter_item.name.truncate(64), letter_item.url, title: t('jump'), target: "_blank" %>
    </span>

    <%= render 'pages/data/icons', resource: letter_item %>
  </div>
<% end %>
