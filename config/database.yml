default: &default
  adapter: postgresql
  encoding: unicode
  pool: 25 # like sidekiq threads count

development:
  <<: *default
  database: forum_reader_development

test:
  <<: *default
  database: forum_reader_test

production:
  <<: *default
  database: forum_reader_production
